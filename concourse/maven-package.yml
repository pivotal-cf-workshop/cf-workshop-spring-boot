platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: latest

inputs:
  - name: git-repo
  - name: version
  
parameters:
  - vn: version/number

outputs:
- name: artifacts
  
run:
  path: git-repo/concourse/package2.sh
  params: 
    version: version/number
