platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: latest

inputs:
  - name: git-repo
  - name: version
  
outputs:
- name: artifacts
  
run:
  path: bash  
  args: [./git-repo/concourse/package2.sh, version/number]
  
params: {file: version/number}
