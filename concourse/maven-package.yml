platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: latest

inputs:
  - name: git-repo
  - name: version
  
outputs:
- name: artifacts
  
run:
  path: cat
  args: [version/number]
  
params: {file: version/number}
