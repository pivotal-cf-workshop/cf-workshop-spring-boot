platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: latest

inputs:
  - name: git-repo
  - name: version

outputs:
- name: artifacts
  
run:
  path: git-repo/concourse/package2.sh
  args: [version/number]
  #params: {version: [version/number]}
